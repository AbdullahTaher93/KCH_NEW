<Window x:Class="KCH_New.Views.SearchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="البحث عن قائمة"
        Height="650" Width="950"
        WindowStartupLocation="CenterOwner"
        FlowDirection="RightToLeft"
        FontFamily="Segoe UI"
        Style="{StaticResource MaterialDesignWindow}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="160"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <!-- Search Bar -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="140"/>
                <ColumnDefinition Width="140"/>
                <ColumnDefinition Width="100"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="txtSearch" Grid.Column="0" Margin="0,0,10,0"
                     materialDesign:HintAssist.Hint="ابحث برقم القائمة أو اسم الزبون..."
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     TextChanged="TxtSearch_TextChanged"
                     FontSize="14"/>
            <RadioButton x:Name="rbById" Grid.Column="1" Content="رقم القائمة"
                         IsChecked="True" VerticalAlignment="Center" Margin="8,0"/>
            <RadioButton x:Name="rbByName" Grid.Column="2" Content="اسم الزبون"
                         VerticalAlignment="Center" Margin="8,0"/>
            <Button Grid.Column="3" Content="بحث"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Click="BtnSearch_Click"/>
        </Grid>

        <!-- Results List -->
        <materialDesign:Card Grid.Row="1" Margin="0,8" Padding="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="نتائج البحث" FontWeight="Bold"
                           Foreground="{DynamicResource PrimaryHueMidBrush}"
                           VerticalAlignment="Center"/>
                <DataGrid x:Name="dgResults" Grid.Row="1"
                          AutoGenerateColumns="False" IsReadOnly="True"
                          SelectionChanged="DgResults_SelectionChanged"
                          Style="{StaticResource MaterialDesignDataGrid}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="رقم القائمة" Binding="{Binding Id}" Width="100"/>
                        <DataGridTextColumn Header="اسم الزبون" Binding="{Binding CustomerName}" Width="*"/>
                        <DataGridTextColumn Header="التاريخ" Binding="{Binding Date, StringFormat=yyyy/MM/dd}" Width="120"/>
                        <DataGridTextColumn Header="الصافي" Binding="{Binding NetPrice, StringFormat=N0}" Width="100"/>
                        <DataGridTextColumn Header="المتبقي" Binding="{Binding Remaining, StringFormat=N0}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </materialDesign:Card>

        <!-- Invoice Detail -->
        <materialDesign:Card Grid.Row="2" Margin="0,4" Padding="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="اسم الزبون:" FontWeight="Bold" FontSize="12"/>
                        <TextBlock x:Name="lblCustomer" FontSize="13"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="التاريخ:" FontWeight="Bold" FontSize="12"/>
                        <TextBlock x:Name="lblDate" FontSize="13"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="الصافي / المدفوع / المتبقي:" FontWeight="Bold" FontSize="12"/>
                        <TextBlock x:Name="lblFinancial" FontSize="13"/>
                    </StackPanel>
                    <StackPanel Grid.Column="3">
                        <TextBlock Text="ملاحظات:" FontWeight="Bold" FontSize="12"/>
                        <TextBlock x:Name="lblNotes" FontSize="13"/>
                    </StackPanel>
                </Grid>
                <DataGrid x:Name="dgItems" Grid.Row="1"
                          AutoGenerateColumns="False" IsReadOnly="True"
                          Style="{StaticResource MaterialDesignDataGrid}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="#" Binding="{Binding ItemNumber}" Width="50"/>
                        <DataGridTextColumn Header="اسم المادة" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="الكمية" Binding="{Binding Quantity}" Width="80"/>
                        <DataGridTextColumn Header="سعر المفرد" Binding="{Binding UnitPrice, StringFormat=N0}" Width="100"/>
                        <DataGridTextColumn Header="الإجمالي" Binding="{Binding TotalPrice, StringFormat=N0}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </materialDesign:Card>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Content="فتح / تعديل" Width="140" Margin="8,0"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Click="BtnOpen_Click"/>
            <Button Content="طباعة" Width="120" Margin="8,0"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Background="#00897B" Foreground="White"
                    Click="BtnPrint_Click"/>
            <Button Content="إغلاق" Width="100" Margin="8,0"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Click="BtnClose_Click"/>
        </StackPanel>
    </Grid>
</Window>
