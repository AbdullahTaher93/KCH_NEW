<Window x:Class="KCH_New.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="نظام KCH - تسجيل الدخول"
        Height="480" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        FlowDirection="RightToLeft"
        FontFamily="Segoe UI">
    <Window.Resources>
        <Style x:Key="InputStyle" TargetType="TextBox" BasedOn="{StaticResource MaterialDesignOutlinedTextBox}">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,8"/>
        </Style>
    </Window.Resources>

    <Border CornerRadius="16" Background="White">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.3"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="180"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="50"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" CornerRadius="16,16,0,0"
                    Background="{DynamicResource PrimaryHueMidBrush}">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <materialDesign:PackIcon Kind="AccountCircle" Width="60" Height="60"
                                             Foreground="White" HorizontalAlignment="Center"/>
                    <TextBlock Text="نظام إدارة القوائم - KCH"
                               Foreground="White" FontSize="18" FontWeight="Bold"
                               HorizontalAlignment="Center" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

            <!-- Form -->
            <StackPanel Grid.Row="1" Margin="32,24">
                <TextBox x:Name="txtUsername"
                         materialDesign:HintAssist.Hint="اسم المستخدم"
                         Style="{StaticResource InputStyle}"/>

                <PasswordBox x:Name="txtPassword"
                             materialDesign:HintAssist.Hint="كلمة المرور"
                             Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                             FontSize="14" Margin="0,8"
                             KeyDown="TxtPassword_KeyDown"/>

                <TextBlock x:Name="txtError" Foreground="Red"
                           FontSize="12" Margin="0,4" Visibility="Collapsed"/>

                <Button x:Name="btnLogin"
                        Content="تسجيل الدخول"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Background="{DynamicResource PrimaryHueMidBrush}"
                        Foreground="White"
                        Height="44" FontSize="15" Margin="0,16,0,0"
                        Click="BtnLogin_Click"/>

                <Button x:Name="btnChangePassword"
                        Content="تغيير كلمة المرور"
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        Height="40" FontSize="13" Margin="0,8,0,0"
                        Click="BtnChangePassword_Click"/>
            </StackPanel>

            <!-- Footer / Drag + Close -->
            <Grid Grid.Row="2" Background="#F5F5F5">
                <Border CornerRadius="0,0,16,16" Background="Transparent"
                        MouseLeftButtonDown="Window_MouseDown"/>
                <Button HorizontalAlignment="Left" VerticalAlignment="Center"
                        Margin="12,0" Style="{StaticResource MaterialDesignIconButton}"
                        Click="BtnClose_Click" ToolTip="إغلاق">
                    <materialDesign:PackIcon Kind="Close" Foreground="Red"/>
                </Button>
                <Button HorizontalAlignment="Right" VerticalAlignment="Center"
                        Margin="12,0" Style="{StaticResource MaterialDesignIconButton}"
                        Click="BtnMinimize_Click" ToolTip="تصغير">
                    <materialDesign:PackIcon Kind="WindowMinimize"/>
                </Button>
            </Grid>
        </Grid>
    </Border>
</Window>
